# Radar de Grupos — Work State

## Objective
- Ferramenta independente (não é Vitrine Criativa nem Agenda Creator): descobrir, organizar e acompanhar grupos públicos do Facebook do nicho do usuário.
- Acesso das lojas/papelarias via login próprio (Supabase Auth).

## Important Details
- Repo: `agendacreator-pro/Radar-de-grupos` (projeto separado da vitrine)
- Supabase project `yhjmulicddyclnqmodlk` — URL `https://yhjmulicddyclnqmodlk.supabase.co`; chaves anon/publishable + service_role estão em `.env` (não commitado). Use o publishable key (`sb_publishable_...`) como `VITE_SUPABASE_PUBLISHABLE_KEY`.
- Worker: `https://radar-de-grupos.meellcriativa.workers.dev` (conta `meellcriativa`, mesmas credenciais wrangler da vitrine)
- Build: `npm run build` (Vite lê `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` do `.env`). Não setar essas vars manualmente no CLI.
- Deploy frontend: `npx wrangler deploy -c wrangler.jsonc` (config commitado define `name`, `main`, `assets` e as vars plain-text `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` — deploy determinístico; NÃO usar `--var` solto, que um deploy sem vars limpa).
- **Worker needs `SUPABASE_SERVICE_ROLE_KEY`** (server functions gravam via service role): já setado via `npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY --name radar-de-grupos` (Wrangler account `meellcriativa` tem as credenciais). **`TAVILY_API_KEY` já setado** (fonte primária, planos com $5-1.000 créditos/mês grátis). Opcional: `BRAVE_API_KEY`.
- **Descoberta roda no Worker Cloudflare** via server functions (`src/lib/radar-engine.ts`): egresso do Supabase (edge functions) bloqueia DuckDuckGo/Facebook → busca retornava 0 grupos. No Worker Tavily/Brave/DDG/Bing funciona; Facebook direto está bloqueado (400) → contagem de membros vem de snippets (nunca inventada).
- Edge Function `radar-grupos` (egresso Supabase) ficou OBsoleta para busca — mantida, mas a UI chama o engine do Worker. `radar-grupos` deployada em `yhjmulicddyclnqmodlk` (usa `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` auto-injetados; `supabase-js` **pinado** em `2.45.4` no `_shared/radar.ts`).
- PowerShell: usar `; if ($?)`; `Select-Object -First`.
- Usuário exige: sempre commit e push sem pedir (no repo do radar).

## Work State

### Completed
- Projeto Supabase `radar-de-grupos` criado + migração `20260907000000_radar_grupos.sql` aplicada (tabelas `radar_grupos`, `radar_grupo_usuario`, `radar_listas`, `radar_lista_grupos`, `radar_buscas` + RLS owner).
- Types regenerados do banco (`supabase gen types typescript --project-id yhjmulicddyclnqmodlk`).
- Edge function `radar-grupos` deployada (401 sem auth; usuário autenticado por Bearer JWT via service role).
- Frontend standalone (TanStack Start) com: landing, `/auth` (login/criar conta/esqueci senha), `/painel/grupos` (página do radar), layout com logout.
- Worker `radar-de-grupos` deployado com vars `SUPABASE_URL` + `SUPABASE_PUBLISHABLE_KEY`.
- HTML do Facebook: scraping via User-Agent; busca via DuckDuckGo HTML.
- Teste: endpoints respondem 200 e `/auth` renderiza título.
- **Engine de descoberta no Worker** (`src/lib/radar-engine.ts`): server functions `radarSearch`/`radarImport`/`radarRecheck` que rodam no Worker (egresso CF). Fontes: Tavily (se `TAVILY_API_KEY`) → Brave (se `BRAVE_API_KEY`) → DDG html → DDG lite → Bing; parse de contagem de membros em snippets. `painel.grupos.tsx` usa o engine (passa o JWT do usuário na chamada; grava via service role). Corrige a busca que retornava 0 grupos (egresso do Supabase bloqueado).
- **Botão Buscar funciona com somente termos extras** (campo principal vazio): `runSearch` valida `term || selectedTerms.length > 0`; botão habilita com termos selecionados.
- `tsc --noEmit` passa; `vite build` gera `.output` (preset cloudflare-module). Deploy frontend: `npx wrangler deploy -c wrangler.jsonc` (raiz, vars plain-text commitadas) OU `cd .output && npx wrangler deploy` (usa wrangler.json gerado; o deploy recente usou esta rota com sucesso).
- **FIX CONFIG (raiz do "Missing SUPABASE_URL")**: o worker tinha o secret `SUPABASE_SERVICE_ROLE_KEY`, mas as vars plain-text `SUPABASE_URL`/`SUPABASE_PUBLISHABLE_KEY` não persistiam em deploys sem `--var` → `radarSearch` (admin client) lançava "Missing Supabase environment variable(s): SUPABASE_URL". Solução: `wrangler.jsonc` commitado (raiz) com as vars; deploy = `npx wrangler deploy -c wrangler.jsonc`. Verificado E2E: busca real no worker retorna grupos e persiste no `yhjmulicddyclnqmodlk` (projeto do radar).
- Erros de ambiente agora logam detalhes técnicos no console do worker e mostram mensagem amigável ao usuário; engine retorna `success:false` em falha de persistência em vez de mascarar como "0 grupos".
- **FIX SUBREQUESTS (limite ~50/invocação)**: buscas volumosas (30 queries Tavily + ~206 grupos) falhavam com "Too many subrequests by single Worker invocation" no `persistGroups` (select + bulk insert + merges + attach + load avulsos). Solução: migration `20260908000000_radar_upsert_rpc.sql` cria a função `radar_upsert_groups(uuid, text[], jsonb)` (SECURITY DEFINER, execute SÓ para service_role; revogado de public/anon/authenticated) que faz insert + merge (ON CONFLICT url) + attach (`radar_grupo_usuario`) + load em **1 subrequest** e devolve `{inserted, groups}`. `persistGroups` = RPC-first com fallback legado (`persistGroupsLegacy`) se a função não existir no DB; `radarSearch`/`radarImport` usam `rows` do RPC direto (sem `loadGroupsByIds` extra). Aplicada via `supabase db push` (CLI já rastreia `20260907000000`). Verificado em escala: sucesso com 189 grupos, `batch_rpc_logs=0`, `subrequest_logs=0`, `exceptions=0`. Note: `supabase db push` no repo do radar precisa de confirmação `[Y/n]` interativa. Teste de stress: `.e2e-search.mjs` (não commitado; usa usuário `radar-e2e-opencode@example.com`).
- **Áudios — resolve DIRETO no browser + fallback Deezer + campo provider** (commit `b898b57`, worker `bb118cf0`): o botão "Ouvir prévia" e a busca manual "Buscar faixa real" agora rodam no navegador via `clientResolveAudio` (importado de `instagram-radar-engine.ts`): busca no iTunes (CORS liberado) e, se nada vier com preview, cai no **Deezer** (`api.deezer.com/search`, dedupe + scorting título/artista, sempre prefere com preview). O match é salvo por persistência via nova ação `instaRadarAudioClientResolve` (persiste a linha: nome/artista/preview/artwork/link/track/artist/provider). O resolve do runtime (`resolveItunesAudio`) ganhou o MESMO fallback Deezer (`pickDeezer`) p/ o enriquecimento automático da corrida. Novo campo `provider` (`itunes`|`deezer`) — migration **`20260913000000_insta_audio_provider.sql`** (aplicada via `supabase db push`) e card mostra badge "🎵 prévia via Deezer" + link externo dinâmico Apple Music/Deezer (`providerLabel`). Tipos: `ItunesMatch.provider`, `InstaAudio.provider`, `toAudioRow` grava provider. eslint/tsc/build limpos.
- **Zerar pesquisas no header + reset de scan** (commit `2d7b1b2`, worker `0feba390`): o botão de zerar o radar (lixeira `instaRadarWipe`) agora fica no header do `/painel/instagram` ao lado de "Rodar radar"/"O que postar hoje?", com confirmação de 6s e auto-revert (mesmo padrão de Grupos/Oportunidades). Removido o botão duplicado do rodapé. `instaRadarWipe` apaga `insta_radar_trends/audios/history/alerts/plan/config` + `insta_conexao` do usuário; como a config é apagada, `next_run_at` volta a `null` → **pode rodar o radar na hora** (sem esperar o throttle de 10 min). O botão já existia no rodapé antes deste commit.

### Pending / To verify
- **Login com Facebook (OAuth) — IMPLEMENTADO e deployed, mas PROVEDOR DESATIVADO no Supabase**: o botão "Continuar com o Facebook" em `/auth` chama `supabase.auth.signInWithOAuth({ provider:'facebook', redirectTo: origin })` (fluxo redirect; se o usuário já estiver logado no FB do navegador, volta direto sem digitar e-mail/senha). Requer (dashboard Supabase → Authentication → Providers → Facebook): ativar e colar **App ID** + **App Secret** de um app do Facebook. No app do Facebook (developers.facebook.com, produto "Facebook Login"): **Valid OAuth redirect URI** = `https://yhjmulicddyclnqmodlk.supabase.co/auth/v1/callback`. Também conferir Site URL (para o redirectTo `https://radar-de-grupos.meellcriativa.workers.dev`); em modo dev do FB, só admins/developers/testers conseguem logar (a liberação pública exige revisão dos escopos public_profile/email). Sem App ID o botão dá erro "provider not enabled" → toast amigável.
- **Cores "estilo Facebook" aplicadas**: paleta trocada de roxo (oklch hue 305) para azul Facebook `#1877F2` (oklch ~0.55 0.2 257.5) em `src/styles.css` (+ `.primary` da página de erro estática em `src/lib/error-page.ts`). Botão FB usa `bg-[#1877F2] hover:bg-[#166FE5]` com SVG inline do "f". Types do Supabase regenerados (`supabase gen types typescript`) após a migration do RPC, para `.rpc("radar_upsert_groups", ...)` tipar.
- `BRAVE_API_KEY` opcional (fonte secundária); corroborar buscas com Tavily (chave `tvly-dev-*` já setada): DDG voltou a dar 202 no IP do worker → Tavily resolve.
- UI: seleção de agência para serviços de envio NÃO se aplica (radar não usa envio).
- **Oportunidades — prioriza recência** (commit `9ff84e3`, worker `48aba59c`): engine extrai `data_publicacao` do título/snippet (PT/EN: dd/mm/aaaa, aaaa-mm-dd, "N de mês", relativos há/ontem/hoje, com teto de 4 anos) + confirma top 6 via `article:published_time`/`created_time` na página do post (guarda `verificado`). Busca com `recentDays` aplica janela de frescor (DDG `df=`, Brave `search_freshness`, Tavily `days`+`sort_by:recency`) e oculta posts antigos conhecidos (`antigas_ignoradas`). UI: chip "Somente últimas 7 dias", badge "recente" + "Publicada X · (verificada|estimada)" no card, filtro "Período" e sort "Mais recentes" usam `data_publicacao` (fallback `data_encontrada`). Nota: `tsc --noEmit` do repo foi limpo (erros pré-existentes corrigidos: tipos `tipo_intencao`, patch de status no hook, `id` em resultados da busca — busca ao vivo agora tem `id` estável para atualizar status).
- **Oportunidades — datas REAIS (filtros honestos)** (commit `a57c6c5`, worker `51203a12`): usuário quis filtrar por ano/semana/mês/24h "sem chute". Removida heurística "fictícia" (marcadores de comentário `8y`/`3m` e "mês de AAAA" não viram mais data). Datas REAIS vêm de: `published_date` do Tavily, `age` do Brave (`providerAgeToIso`), e page-meta (`article:published_time`/`created_time`) quando o egresso ao Facebook funciona (geralmente bloqueado/400) — todas marcam `verificado`. Filtros "Período" (24h/7d/30d) + "Ano" na lista usam **somente** datas confirmadas; posts sem confirmação caem num grupo separado "sem data confirmada" com aviso (não são contados, para não criar resultado fictício). Badge "recente" e sort "Mais recentes" idem (só verificadas; fallback `data_encontrada` no sort). Engine não descarta por data estimada (só confirmada-antiga). Provider `age` do Brave: `3 months ago` NON é horas (ancora `/^h/`).
- **Termos sugeridos foco papelaria/miolo** (commit novo): `RADAR_SUGGESTED_TERMS` reordenado/ampliado para o nicho de conversão do usuário (papelaria personalizada — vende programa de criação de agendas): topo = `miolo de agenda`, `agenda 2027`, `papelaria personalizada`, `arquivos digitais`, `agenda personalizada`, `papelaria digital`, `kits digitais`, `encadernação` (os 8 primeiros viram chips nas páginas `/painel/grupos` e `/painel/oportunidades`). Ajuste refletido sem deploy especial (só frontend).
- **Grupos — sinal automático "Posso postar"** (commit novo): `painel.grupos.tsx` ganhou `grupoPostingInfo()` que analisa a `description` (snippet da busca) para detectar sinais de regras de postagem (`regras`/`proibido`/`sem divulgação`/`sem spam`, aprovação/moderação/pendente) → níveis `livre | regras | moderacao | ambos | sem_info`. Badges nos cards, coluna "Postagem" na tabela e aviso com motivos no detail. Filtro "Posso postar" com chips: **Público e livre** (is_public + sem sinal de regra OU marcado como permite_divulgacao), **Permite divulgação (marcado)**, **Com sinal de regra**. Card de estatística "Posso postar" substituiu "Sou membro". Honestidade: sinal é lido da descrição vista na busca; texto em UI orienta conferir as regras no grupo antes de postar. Via RPC/DB não muda — tudo client-side; deploy frontend `npx wrangler deploy -c wrangler.jsonc`.
- **Grupos — visão "Simples" + foco em grupos livres** (commit novo): página agora abre em **view `resumo`** (lista enxuta: nome clicável + membros + badge Público/Privado + botão Detalhes) e com o filtro **"Posso postar" padrão = `posso`** (Público e livre = is_public e sem sinal de regra na descrição, ou marcado como permitindo divulgação). Toggle Simples/Cards/Tabela; demais filtros inalterados.
- **Grupos — zerar carteira + buscar só novos** (commit novo): `radarWipe` (server fn) apaga os vínculos `radar_grupo_usuario` e `radar_lista_grupos` do usuário + grupos órfãos (`radar_grupos` sem nenhum usuário) — botão lixeira no header com confirmação (6s). Busca tem modo **"Só novos grupos" (padrão ON)**: `radarSearch` com `newOnly` carrega os URLs que o usuário já tem, exclui-os ANTES do persist (sem re-salvar/repetir) e retorna `repetidos_ignorados` (badge "X repetidos ocultados"). USO: click lixeira → Confirmar limpar → Buscar com termos do nicho.
- **Zero total + wipe nas Oportunidades** (commit novo): `radarWipe` agora também apaga `radar_listas`, `radar_buscas` e reseta filtros/view no client (lista também apaga grupos órfãos). Novo `oportunidadesWipe` (server fn em `radar-oportunidades-engine.ts`) apaga as `radar_oportunidades` do usuário; botão lixeira adicionado no header de `/painel/oportunidades` (confirmação 6s, reseta `lastResults`/filtros e invalida `radar-oportunidades`).
- **Grupos — cards de estatísticas clicáveis** (commit novo): "Grupos salvos" (limpa filtros), "Posso postar" (postingFilter=posso), "Favoritos" (novo filtro `soFavoritos`) e "Mais de 100 mil membros" (minMembers=100000) filtram a lista e rolam até ela; card ativo com anel primário.
- **Groups grid OK**: tabela da visão "Tabela" foi convertida de `<table>` p/ **grade CSS proporcional** (`TABLE_GRID` em `painel.grupos.tsx`): colunas `minmax(8rem…1.4fr)`, cabeçalhos `nowrap`, textos quebram na palavra NUNCA truncados, sem rolagem lateral.
- **PAINEL EM LARGURA TOTAL**: o utilitário global `app-shell` limita tudo a 30rem (480px centralizado) — era a causa do "embolado". Criei `painel-shell` (`min-height:100dvh; max-width:none`) em `styles.css` e passei a usar no layout `painel.tsx` (auth/vitrine continuam com app-shell). A tabela agora aproveita a tela toda.
- **BUSCA SEMPRE COM RESULTADOS NOVOS** (commit `dcc8ad8`): cada busca gira as consultas — `rotateWindow`+`seededShuffle` em `radar-engine.ts`. Grupos: pool de **11 templates** de query, usa 4 por termo com offset rotativo (seed=Date.now) e tasks embaralhadas (antigamente as mesmas 3 queries → top-20 sempre idênticos). Oportunidades: **12 frases de intenção** (`INTENT_PHRASES`), usa janela de 4 por busca girando. `max_results` 20→25 (Tavily/Brave), `MAX_QUERIES` 42→44. Mesmo com fontes (Tavily/Brave/DDG/Bing) — Facebook direto bloqueia o egresso do Worker, a "varredura do Facebook" é feita via indexadores públicos.

### Radar do Algoritmo — Instagram (módulo 📡, commit `f8c415c`)
- **SQL NÃO APLICADO (usuário roda manualmente)**: `supabase/migrations/20260911000001_insta_radar.sql` cria `insta_radar_config` (nicho/keywords + status/last_run/next_run/alert_count), `insta_radar_trends`, `insta_radar_audios`, `insta_radar_history` (snapshot jsonb por execução), `insta_radar_alerts`, `insta_radar_plan` (calendário semanal) e `insta_conexao` (estado de integração oficial, sem credenciais) — todas RLS `user_id = auth.uid()` owner-only. Rodar em blocos no SQL Editor do Supabase (`yhjmulicddyclnqmodlk`).
- **Módulo independente, honesto (nenhum número inventado)**: descoberta via busca web pública (Tavily→Brave→DDG→Bing) no Worker — API do Instagram/Meta bloqueia o Worker. Escore/ciclo são **estimativas por regras** derivadas de sinais reais de títulos/snippets (viral, crescimento, novidade, engajamento, saturação, recência via `published_date`/`age`); cada linha tem `fonte`: `oficial | observado | estimativa | inferencia` (badges na UI). Componentes: "Tendências do momento" (cards com score/compat/ciclo/formato/motivo/adaptação), **"O que postar hoje?"** e **"Transformar trend"** (MEELL = inferência determinística: formato, gancho rotativo do dia, ideia, texto na tela, roteiro, legenda, CTA, hashtags, capa, objetivo — sem views/engajamento fabricados), **Áudios em alta** (artista/usos observados OU "dado não disponível"), Emergentes vs Saturadas, Ganchos, Alertas automáticos (nova tendência, áudio, nicho ≥75%, saturação de auge), Histórico de análises, **Calendário semanal** (planejamento), keyword chips do nicho (compat %), e seção **Conectar Instagram (Meta)** mostrando o que precisa ser configurado (app Meta + permissões `instagram_basic`/`instagram_manage_insights`/`pages_read_engagement`) — sem conexão, UI não inventa métricas oficiais.
- **Novas tabelas fora do `Database` types** → engine usa `admin as any` (+ pragma eslint local, mesmo padrão de `radar-engine.ts`). 🔑 Tudo por `verifyUser(token)`; throttle **10 min** entre execuções do mesmo usuário; `QUERY_BUDGET=8`, wall 35s. Rota: `/painel/instagram` (aba "Instagram" em `painel.tsx`).
- Execução: `npm run build` → `cd .output && npx wrangler deploy --name radar-de-grupos`. Worker deployado (versão `294b2f04`), `/painel/instagram` retorna 200 com título. `tsc --noEmit` e `eslint` (dos arquivos do módulo) limpos.
- **Áudios com prévia REAL (reprodução no painel)**: migration `20260912000000_insta_audio_preview.sql` (aplicada via `supabase db push`) adiciona `preview_url/artwork_url/itunes_url/track_name/artist_name/enrich_attempted_at` em `insta_radar_audios`. O worker vincula cada áudio a uma faixa real do **iTunes Search API** (preview oficial 30s, sem inventar): `resolveItunesAudio` + `rankItunesPick` em `instagram-radar-engine.ts`; top 8 novidades são enriquecidas na corrida do radar (best-effort) e qualquer áudio sem prévia tem a ação sob demanda `instaRadarAudioPreview` (hook `useInstaRadarAudioPreview`). UI: card de áudio mostra capa (ou gradiente IG), nome limpo p/ Reels (track_name ?? nome com badge do nome bruto), botão **Ouvir prévia** (player `<audio>`), **Copiar nome p/ Reels**, links Apple Music/YouTube e dica de como achar no editor de Reels. Se não achar a faixa, avisa honestamente (nenhuma música inventada).
- **Ranking + post completo do áudio**: seção "Áudios em alta" lista TODAS as músicas mapeadas + **Ranking numerado** (`AudioRankRow`, 1º a N pela tendência observada) com Ouvir (player inline)/Ler nome (speechSynthesis pt-BR)/Copiar nome/Post. `instaRadarAudioPost` + `buildAudioPost` geram o post completo do áudio (legenda, hashtags do nicho+música, **horários de postagem em alta** = `buildHorarios` estimativa de pico rotativa rotulada como estimativa, roteiro, gancho, texto na tela, CTA, capa, objetivo) — hook `useInstaRadarAudioPost`; `InstaContent.horarios` opcional renderizado no ContentDialog e incluído no "Copiar tudo".
- **FIX extração de áudios em alta** (commit `d952a18`): o extractor antigo pegava lixo ("usar", "em", "s em alta no Reels: artistas como...") → iTunes devolvia música aleatória. Novo `extractAudioMeta` extrai nome/artista reais com regras rígidas em ordem de confiança (1º título entre aspas, 2º rótulo explícito `áudio:/música:/trend de áudio:`, 3º `audio Nome de Artista`), com filtro `isAudioGeneric` (stoplist de palavras "em/bombando/semana/instagram/...") que descarta trechos genéricos — o que não parecer um nome real vira `null` (card não lista, em vez de mostrar lixo). Re-execução re-enriquece top 8 sem preview (o filtro `prevAudioSet` foi removido).
- **FIX "prévia não encontrada"** (commit `4a6d1a0`, worker `aa47113f`): `resolveItunesAudio` agora tenta **variantes do termo** — `searchTermVariants` (título entre aspas extraído de nome-lixo antigo → base sem rótulo `áudio/música/som` → primeira oração → artista limpo) em vez de 1 query só; agrega resultados com peso por variante + pontuação nome/artista; **sempre prefere resultado com `previewUrl`** (antes, o melhor-scored SEM preview travava o botão). No card, falha da busca automática abre direto o bloco "Não achou? Buscar por outro nome" (`instaRadarAudioResolve` — digita nome/artista corrigido e salva na linha). Stored-rows antigas de lixo continuam no DB: corrigir via busca manual ou lixeira + re-rodar. eslint/tsc/build limpos.
- **RANKING REAL COM HISTÓRICO E LATÊNCIA** (migration `20260914000000_insta_audio_rank.sql` APLICADA via `supabase db push`; worker `0c15e361-995e-46f7-b589-367a4c3f7dfa`): `insta_radar_audios` ganhou `album`, `provider_id`, `track_url`, `trend_status`, `rank`, `previous_rank`, `rank_change`, `score_delta`, `growth_rate`, `first_seen_at`, `last_seen_at`, `seen_count` + índice `(user_id, rank)`; `insta_radar_config` ganhou `auto_update` (default true). No `runAnalysis` do radar: cada execução lê o snapshot da vitrine anterior (`insta_radar_history.resumo.audios`) → ordena por **score (Trend Score)** → grava `rank`/`previous_rank`/`rank_change`, `score_delta` (vs. snapshot), `growth_rate` (% de usos vs. row anterior, só quando há números das 2 execuções — senão `null`), `seen_count`+`first/last_seen_at` e `trend_status` (`viral | crescendo | nova | consolidada | perdendo`, de `deriveAudioTrendStatus`). O `resumo.audios` de cada história vira o snapshot da próxima (variação entre coletas honesta, nada fabricado). Enriquecimento automático subiu de top 8 → **top 15** via `applyMatch` (album+provider_id+track_url). `loadConfig`/dashboard ganharam `auto_update` + `auto_run_hint`; nova server fn `instaRadarSaveAutoUpdate` (hook `useInstaSaveAutoUpdate`) só se o usuário ligar. UI `/painel/instagram`: **sub-abas Ranking/Cards**, chips de filtro por `trend_status`, badge de status em cada linha/card, `RankChangeBadge` (▲/▼ posições), variação de score (pts) e % de usos, capa na linha do ranking, badge do álbum, toggle "Auto atualização: ON/OFF" no header, nota honesta consolidada no card (prévia legal 30s ≠ Reel original). `speakNome` corrigido (não fala mais "Artista não identificado"). eslint/tsc/build limpos.
- **FIX estilos (g�neros) NULOS + lixo do navegador de �udio** (novo, worker `67bd8b32`): no worker o iTunes RSS falha e o chart usava o Deezer, cujo `/chart/0/tracks` NAO exp�e `genre_id` -> ranking "Por Estilo" ficava vazio. Agora o `fetchDeezerChart` resolve o g�nero real por **�lbum �nico** (`api.deezer.com/album/{id}` ? `genres.data[0].name`/`genre_id`, dedupe + chunks de 6); `fetchItunesTopSongs` extrai o id num�rico da faixa da URL do RSS (`i=`) p/ o lookup (preview + `primaryGenreName`) e manda UA de browser. Novo fallback `inferAudioGenre` (classificador determin�stico PT-BR) p/ nada ficar sem estilo quando a fonte oficial n�o exp�e; `applyMatch` herda o g�nero oficial do match (campo `genre` novo em `ItunesMatch`). Merge chart�observado junta o melhor dos dois (estilo/preview/score). Novo filtro `isJunkInstaAudio` (+ blacklist de r�tulos de UI do Instagram: Em destaque/Explorar/Populares/Usar �udio/Hypado/Para voc�/novoqueridinho/...) e guarda de URL `instagram.com/popular|explore` -> some o lixo de �udios E das tend�ncias ?? sint�ticas; histerese p/ frases truncadas (`la�ou`, `ah mas...`). Verificado E2E no worker: 22/25 audios com g�nero real (Samba/Pagode, Sertanejo, M�sica Religiosa...), trends ca�ram de 58 com lixo p/ 44 reais. tsc/eslint/build limpos. Deploy: `npx wrangler deploy -c wrangler.jsonc`. Helpers `.radare2e.mjs`/`.dashcheck.mjs` ignorados (glob `.*.mjs` no .gitignore).
